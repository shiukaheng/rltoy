[project]
name = "rltoy"
version = "0.1.0"
description = "Readable reinforcement-learning algorithms on pedagogical Gymnasium environments"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bettermdptools>=0.8.6",
    "gymnasium[toy-text]>=0.26.3",
    "matplotlib>=3.11.1",
    "pygame>=2.6.1",
    "torch>=2.13.0",
    "tyro>=1.0.15",
]

[project.scripts]
rltoy-q-learning = "rltoy.algorithms.q_learning:main"
rltoy-deep-q-learning = "rltoy.algorithms.deep_q_learning:main"
rltoy-actor-critic = "rltoy.algorithms.actor_critic:main"
rltoy-gae = "rltoy.algorithms.gae:main"
rltoy-reinforce = "rltoy.algorithms.reinforce:main"
rltoy-reinforce-baseline = "rltoy.algorithms.reinforce_baseline:main"
rltoy-sarsa = "rltoy.algorithms.sarsa:main"
rltoy-sarsa-lambda = "rltoy.algorithms.sarsa_lambda:main"
rltoy-compare-tabular = "rltoy.experiments.compare_tabular:main"
rltoy-interactive-graph = "rltoy.cli.interactive_graph:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rltoy"]

[tool.uv]
# bettermdptools pins pygame 2.1.0, which predates CPython 3.13 wheels.
override-dependencies = ["pygame>=2.6.1"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
